// App-wide constants
export const APP_NAME = 'Campus Connect';

// Default values
export const DEFAULT_IMAGE_URL = 'https://picsum.photos';

// LocalStorage keys
export const STORAGE_KEYS = {
  STUDENTS: 'students',
  FACULTY: 'faculty',
  EVENTS: 'events',
  ATTENDANCE: 'attendance',
} as const;

// Routes
export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  SIGNUP: '/signup',
  DASHBOARD: '/dashboard',
} as const;
